# =============================================================================
# Prior Authorization Review — Root Docker Compose Environment Variables
# =============================================================================
# Copy this file to .env (in this root directory) and fill in your values:
#   cp .env.example .env
#
# docker-compose.yml substitutes these variables into all FOUR agent containers
# (clinical, coverage, compliance, synthesis). The FastAPI backend reads its
# own env vars from backend/.env (see backend/.env.example).
#
# The .env file is gitignored and will NOT be committed.
# =============================================================================

# ---------------------------------------------------------------------------
# Microsoft Foundry — project endpoint (REQUIRED by all 4 agent containers)
# ---------------------------------------------------------------------------
# Get this from: Microsoft Foundry portal → your project → Home tab
# Format: https://<resource-name>.services.ai.azure.com/api/projects/<project>
AZURE_AI_PROJECT_ENDPOINT=https://<your-foundry-resource-name>.services.ai.azure.com/api/projects/<project>

# ---------------------------------------------------------------------------
# Model deployment name (REQUIRED by all 4 agent containers)
# ---------------------------------------------------------------------------
# The Azure OpenAI deployment name from:
# Microsoft Foundry portal → Build tab → Deployments → deployment name
# Recommended: gpt-5.4
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-5.4

# ---------------------------------------------------------------------------
# MCP Server URLs (optional — production defaults are pre-set in docker-compose.yml)
# ---------------------------------------------------------------------------
# Override only if you want to point at local or custom MCP server instances.
# All MCPs are pre-configured for Anthropic's healthcare gateway. The agents
# inject User-Agent: claude-code/1.0 defensively (Cloudflare blocks the
# default Python-urllib UA but accepts any other identifier).

# MCP_ICD10_CODES=https://hcls.mcp.claude.com/icd10_codes/mcp
# MCP_PUBMED=https://pubmed.mcp.claude.com/mcp
# MCP_CLINICAL_TRIALS=https://hcls.mcp.claude.com/clinical_trials/mcp
# MCP_NPI_REGISTRY=https://hcls.mcp.claude.com/npi_registry/mcp
# MCP_CMS_COVERAGE=https://hcls.mcp.claude.com/cms_coverage/mcp

# ---------------------------------------------------------------------------
# Azure Application Insights (optional — shared by all containers)
# ---------------------------------------------------------------------------
# APPLICATION_INSIGHTS_CONNECTION_STRING=InstrumentationKey=...;IngestionEndpoint=...
#
# OpenTelemetry sensitive-data capture is OFF by default. Each agent honors
# ENABLE_OTEL_SENSITIVE_DATA=true (passed through docker-compose.yml) to attach
# prompts, completions, and tool-call arguments to gen_ai.* spans for local
# debugging. PHI risk — never set this in shared/staging/production
# environments. Default: false.
# ENABLE_OTEL_SENSITIVE_DATA=false
