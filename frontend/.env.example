# Backend API URL (exposed to browser via NEXT_PUBLIC_ prefix)
# Direct connection bypasses the Next.js rewrite proxy, which times out
# on long-running multi-agent reviews (3-5 minutes).
NEXT_PUBLIC_API_BASE=http://localhost:8000/api
