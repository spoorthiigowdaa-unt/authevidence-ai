# Microsoft Agent Framework — refreshed Hosted Agents preview (April 2026+)
# See: https://learn.microsoft.com/azure/foundry/agents/how-to/migrate-hosted-agent-preview
#
# Package split (refreshed preview):
#   agent-framework-core        — Agent / @tool / MCPStreamableHTTPTool / SkillsProvider
#   agent-framework-foundry     — FoundryChatClient (was: agent_framework.azure.AzureAIAgentClient)
#   agent-framework-foundry-hosting — ResponsesHostServer (replaces azure-ai-agentserver-agentframework)
#
# The old `azure-ai-agentserver-agentframework` adapter has been retired and
# MUST NOT be reintroduced — it targets the initial preview backend that is
# being deprecated on 2026-05-22.
agent-framework-core>=1.13.0,<2
agent-framework-foundry>=1.10.4,<2
agent-framework-foundry-hosting>=1.0.0b260730,<2
# Protocol library — pulled transitively by agent-framework-foundry-hosting,
# pinned here for reproducibility.
azure-ai-agentserver-core>=2.0.0,<3
# Managed identity / DefaultAzureCredential
azure-identity>=1.15.0,<2
# Shared httpx client used by all in-container MCPStreamableHTTPTool instances
# (sets User-Agent and timeout).
httpx>=0.27.0,<1
# MCP runtime — required by `MCPStreamableHTTPTool` from agent-framework-core.
# NOT pulled transitively in the current preview, must be pinned explicitly
# (mirrors agents/coverage/requirements.txt). Upper bound matches the range
# agent-framework-foundry-hosting is built against — mcp 2.x is incompatible.
mcp>=1.24.0,<2
# Environment variable loading
python-dotenv>=1.0.0,<2
# Azure Monitor OpenTelemetry (optional — remove if not using App Insights)
azure-monitor-opentelemetry>=1.6.0,<2
